# Synerixis AI 智能客服 - 安装与运行

## 安装与运行先决条件

- .NET SDK 8.0+
- Node.js 18+ (for frontend)
- PostgreSQL (or SQL Server)
- API Keys: OpenAI / 阿里云大模型

## 步骤

### 1. 克隆仓库

```bash
git clone https://github.com/sdrom2008/my-project.git
cd my-project
```

### 2. 后端设置

```bash
cd Synerixis.Api
dotnet restore
dotnet build
# 配置 appsettings.json (添加 DB connection, AI API keys)
dotnet run
```

API 默认运行在 `https://localhost:5001`。访问 `/swagger` 查看 API 文档。

### 3. 前端设置

```bash
cd frontend
npm install
npm start
```

前端默认运行在 `http://localhost:3000`。

### 4. 数据库迁移（如果使用 EF Core）

```bash
dotnet ef migrations add InitialCreate --project Synerixis.Infrastructure
dotnet ef database update --project Synerixis.Infrastructure
```

### 5. 测试

- 单元测试：`dotnet test`
- E2E 测试：使用 Postman 测试 API，或 Cypress for frontend

## 贡献指南

欢迎贡献！请遵循以下步骤：

1. Fork 仓库
2. 创建 feature 分支 (`git checkout -b feature/AmazingFeature`)
3. Commit 变更 (`git commit -m 'Add some AmazingFeature'`)
4. Push 到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 代码风格

- 遵循 .NET 编码规范
- 使用 EditorConfig

## 许可证

本项目采用 MIT License (LICENSE)。

## 联系我们

- GitHub: sdrom2008
- X (Twitter): @sdrom2008
- Email: sdrom2008@qq.com

## 关于 Synerixis AI 智能客服

感谢使用 **Synerixis AI 智能客服**！

我们致力于 AI 驱动的跨境电商平台智能客服解决方案。

> 特别说明：本项目从最初的电商客服系统，演进为专注于**跨境电商平台**的智能客服解决方案，提供更智能、更高效的客户服务体验。
