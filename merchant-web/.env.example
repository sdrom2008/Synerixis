# 浏览器请求的 API 前缀。留空则走同源 /api（开发时由 Vite 代理）——PC 网页端推荐留空
VITE_API_BASE_URL=

# Vite 开发代理目标：必须指向本机正在监听的 API（默认 7092）
# 本机测网页端请用 127.0.0.1，不要填 192.168.x.x（双网卡易踩坑）
VITE_API_PROXY_TARGET=http://127.0.0.1:7092
